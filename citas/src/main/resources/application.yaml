server:
  port: 8080
spring:
  application:
    name: citas

  datasource:
    url: jdbc:mysql://localhost:3306/veterinaria_cita_db?useSSL=false&serverTimezone=UTC
    username: root
    password:

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
