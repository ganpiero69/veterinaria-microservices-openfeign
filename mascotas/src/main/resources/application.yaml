spring:
  application:
    name: mascotas

  datasource:
    url: jdbc:mysql://localhost:3306/veterinaria_mascota_db
    username: root
    password:

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8082